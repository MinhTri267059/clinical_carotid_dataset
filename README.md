# clinical_carotid_dataset
huấn luyện một mạng MLP và CNN từ đầu trên 300 ca (rất dễ bị quá khớp), xu hướng hiện tại là biến đổi tất cả các loại dữ liệu thành định dạng chuỗi (tokens) và đưa vào một mô hình Transformer thống nhất.
